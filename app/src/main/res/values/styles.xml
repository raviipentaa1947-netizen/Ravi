<resources>
    <!-- Base app styles -->
    <style name="OrangeButton" parent="Widget.MaterialComponents.Button">
        <item name="android:background">@drawable/orange_button_bg</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">12dp</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <style name="Widget.SalaryCalculator.EditText" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/primary_variant</item>
        <item name="hintTextColor">@color/secondary_color</item>
    </style>
</resources>
